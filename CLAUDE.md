# CLAUDE.md

このリポジトリは `index.html` を中心とした単一ページのWebアプリ（マーケット用語ターミナル）です。

## GitHub Pagesでの公開（重要）

このアプリは以下のGitHub PagesのURLで公開されています。

https://shuya0908.github.io/market-terms-glossary/

`main` ブランチに push すると、`.github/workflows/pages.yml` のGitHub Actionsワークフローが自動的にビルド・デプロイを行い、上記URLへ反映される。手動でのArtifact同期作業は不要（`index.html` / `terms.json` / `manifest.json` / `icon.png` はリポジトリのファイルがそのまま配信される）。

## 用語を追加する際のカテゴリ見直し（重要）

`terms.json` に新しい用語を追加するたびに、追加した用語だけでなく **用語集全体のカテゴリ分類を見直すこと**。具体的には:

- 新しい用語がどのカテゴリ（`index.html` の `CATS` に定義された `board` / `order` / `market` / `clearing` / `derivative` / `tech` / `compliance` / `front` / `middle` / `back` / `fx` / `bond` / `quant` / `margin` など）に属するかを機械的に決めるのではなく、既存の同カテゴリの用語群と並べて意味的に一貫しているか確認する。カテゴリの正確な一覧は、都度 `index.html` の `CATS` オブジェクトを確認すること（このリストは例示であり、新カテゴリ追加時にここを更新し忘れても実害はない）。
- 追加によって、既存の用語のカテゴリ分けが相対的に不自然になっていないか（例：あるカテゴリに新設した概念グループが増えたことで、既存の類似用語がそのカテゴリへ移した方が一貫性が高くなる、逆に既存カテゴリに収まりきらなくなった、など）を確認し、必要なら既存用語のカテゴリも一緒に変更する。
- 既存のカテゴリでは意味的に無理がある場合は、新しいカテゴリを追加してよい（追加する際は `index.html` の `CATS` オブジェクトに `label` と重複しない `hue` を定義する）。
- カテゴリ変更は `terms.json` の `c` フィールドのみで表現され、`index.html` 側にカテゴリ別の用語リストは存在しない（`CATS` はラベル・色の定義のみ）。そのため用語のカテゴリを変更する場合は `terms.json` を編集するだけでよいが、新カテゴリを追加した場合は `index.html` の `CATS` とREADME.mdのカテゴリ一覧の説明も合わせて更新すること。
- カテゴリを見直した結果を `main` ブランチにpushすれば、GitHub Pagesへは自動的に反映される（上記参照）。

## 用語カードの並び順

用語集（GLOSSARY）画面・マーク済み（WATCHLIST）画面の用語カードは、**カテゴリ順にグループ化して表示する**。実装は `index.html` の `sortByCategory()`（`CAT_ORDER = Object.keys(CATS)` の順で並べ替え）で、`renderGlossary()` と `renderMarked()` の両方から呼び出している。

- 新しいカテゴリを `CATS` に追加する際は、そのカテゴリが並び順のどこに来てほしいかを意識して `CATS` オブジェクト内の追加位置（キーの並び順）を決めること。`CATS` はJavaScriptのオブジェクトなので、キーの定義順がそのまま `Object.keys(CATS)` の順序になり、カード表示順・カテゴリフィルターのチップ表示順の両方に影響する。
- `terms.json`（`BUILTIN_GLOSSARY`）内での用語エントリ自体の並び順はこの表示順に影響しない（表示時に毎回 `sortByCategory()` でソートし直すため）。そのため用語追加時に配列内のどこに挿入するかは、可読性のために近いカテゴリの近くに置く程度でよく、厳密な並び替えは不要。

## 用語を追加する際の参考文献の更新（重要）

`terms.json` に新しい用語を追加する際、その用語の情報源とした参考文献（Webサイト・記事など）を、`index.html` の用語集画面下部にある「参考文献」セクション（`<div class="refs">` 内の `<ul>`）にも追加すること。

- 追加する用語のカテゴリに対応する参考文献が既にリストにある場合（例: 証券用語一般なら日本取引所グループや野村證券の用語集）は、重複して追加しなくてよい。
- 新しい分野・出典（例: 特定の業界団体の公式サイト、専門解説サイトなど）から用語を追加した場合は、その出典を1件追加する。個別記事へのディープリンクではなく、なるべく安定した（リンク切れしにくい）トップページ・用語集トップ・公式ドキュメントのURLを選ぶこと。
- リンクを追加する前に、実際にアクセス可能でリンク切れでないことを確認すること（このセッションでは`WebFetch`が使えない場合があるため、`WebSearch`等で存在を確認できれば足りる）。
- この変更を `main` ブランチにpushすれば、GitHub Pagesへは自動的に反映される（上記参照）。

## 用語を追加する際の穴埋めクイズの見直し（重要）

`terms.json` に新しい用語を追加するたびに、その用語（および関連して定義文を変更した既存用語）について、**穴埋め（記述式）クイズで不適切な単語が出題されないかを必ず確認すること**。穴埋めの空欄となる単語は `index.html` 内の以下のロジックで定義文から自動抽出される（用語集画面にカテゴリ別の出題単語リストのようなものは存在せず、すべてこのロジックの結果に委ねられている）。

- `BLANK_WORD_RE` で定義文から漢字・カタカナ・英字・数字それぞれの連なりを候補として切り出し、`extractBlankCandidates()` が `BLANK_ATOMIC_WORDS`（用語集の見出し語＋手動登録した一般語の辞書）を使って意味のある単語単位に分割する。
- `pickBlankWord()` が、`BLANK_STOPWORDS`（正解が一意に定まらない汎用語）を除外し、かつ用語集全体での出現頻度（DF）が低い＝その用語に固有な語を優先して空欄の正解を選ぶ。
- 候補が1つも残らない用語は穴埋め形式の出題対象から自動的に除外される（`pickBlankWord()` が `null` を返す）。

新しい用語を追加した後は、次の手順で確認すること。

1. 追加・変更した各用語について、`extractBlankCandidates()` の結果（穴埋め候補として抽出される単語）を確認する。実装を直接いじって試すのが手間な場合は、`index.html` の該当ロジック（`BLANK_STOPWORDS` 〜 `pickBlankWord()` 定義部分）を抜き出し、Node.js等で `terms.json` を読み込ませて候補語を一覧出力するスクリプトを一時的に書いて検証してよい（リポジトリに恒久的なテスト用ファイルとして残す必要はない）。
2. 抽出された候補の中に、次のような**不適切な単語**が含まれていないか確認する。含まれる場合は、既存のリストと同じ考え方で `BLANK_STOPWORDS` または `BLANK_BROKEN_WORDS` に追加するか、定義文の言い回しを変えて回避する。
   - その用語を理解していても一意に特定できない汎用語（例：「〜的」「〜等」で終わる修飾語、「〜額」「〜幅」のような抽象名詞など。既存の `BLANK_STOPWORDS` 内のコメントに分類の考え方がまとめてある）。
   - 送り仮名の欠落等により文中の語を正しく切り出せていない断片（例：「規律付け」を「規律付」とだけ切り出してしまうケース）。`BLANK_BROKEN_WORDS` に追加して除外する。
   - 「ΔEVE」「ΔNII」のように、正規表現の文字種判定の都合で記号（Δ等）だけが除かれ、記号抜きでは意味をなさない断片になってしまう語。
   - 接尾辞を省略して答えると誤って不正解になりそうな語（例：「〜側」「〜時」など）。この場合は `BLANK_STOPWORDS` に追加するより、定義文の言い回しを変えて接尾辞なしでも成立する表現に直すほうが望ましい。
3. 併せて、新しい用語を追加・削除したことで**既存の用語**の定義文から新たに問題のある候補が抽出されるようにならないか（`buildDefWordFrequency()` はterms.json全体を再計算するため、追加・削除の両方でDFが変化し、既存用語の穴埋め正解の選ばれ方が変わることがある）を軽く確認する。用語を削除する場合も同様の確認を行うこと。
4. この見直しの結果を `main` ブランチにpushすれば、GitHub Pagesへは自動的に反映される（上記参照）。

## terms.jsonの修正・追加・削除を行った際の履歴記録（CHANGELOGタブ・重要）

用語集（`index.html` の GLOSSARY タブ）とは別に、**「修正履歴」タブ（`tabChangelogBtn` / `panelChangelog`、英語表記 CHANGELOG）** を設けている。用語集本体には常に「現在正しいとされる内容」だけが載るため、いつ・どの用語を・なぜ直した／追加した／削除したかは別画面である修正履歴タブでのみ追える。

これは**リポジトリ管理者（Claude）が `terms.json` に対して行う変更の履歴**であり、閲覧者が「管理」タブ（後述）でローカルに行う追加・編集・削除の操作ログとは別物。閲覧者のローカル操作は各自のブラウザのlocalStorageに閉じており、`changelog.json`（リポジトリで共有・git管理）には記録されない。

- データソースは `terms.json` と同じ階層に置く **`changelog.json`**。`index.html` から `terms.json` と同様に `fetch()` して読み込み、`renderChangelog()`（`index.html` 内）がカードとして描画する。`main` へのpushだけでGitHub Pagesに反映される点も他のデータファイルと同じ（他のファイルと同様に手動同期は不要）。
- スキーマは配列で、各要素は以下のフィールドを持つオブジェクト。
  - `date`: `YYYY-MM-DD` 形式の日付文字列。**日本標準時（JST）** を基準に、その修正・追加・削除を行った日を記録する（`TZ=Asia/Tokyo date +%Y-%m-%d` 等で取得できる）。
  - `type`: `"correction"`（既存用語の定義修正）／ `"addition"`（修正に伴う関連用語の追加）／ `"deletion"`（既存用語の削除）のいずれか。
  - `term`: 対象用語の `t`（表記）。`terms.json` 側の表記と一致させる（`deletion` の場合は削除前の表記）。
  - `c`: 対象用語のカテゴリキー（`terms.json` の `c` と同じ）。タブ上でのタグ色付けに使う。
  - `before`: `type: "correction"` または `"deletion"` の場合に、変更前（削除前）の `d`（定義文）全文。
  - `after`: `type: "correction"` または `"addition"` の場合に、変更後（追加した）`d` の全文。`type: "deletion"` では持たない。
  - `reason`: 修正・追加・削除の理由を簡潔に説明する文（何が不正確だったか、何を根拠に直した／削除したか）。
- **既存用語の `terms.json` の `d` フィールドを書き換えるたびに** `type: "correction"` のエントリを、**既存用語を `terms.json` から丸ごと削除するたびに** `type: "deletion"` のエントリを、それぞれ1件 `changelog.json` の配列に追記すること（末尾に追記すればよい。表示時は日付の新しい順に並べ替えられるため、配列内の並び順自体は気にしなくてよい）。単なる誤字修正のような些末な変更を除き、内容・時制・数値等の実質的な修正、および用語の削除は基本的にすべて記録する。
- 純粋に新規の用語を追加するだけ（既存用語の誤りとは無関係）の場合は、このCHANGELOGへの記録は必須ではない（`terms.json` に追加すれば用語集タブに現れるため）。ただし、次のセクションで述べる「修正に伴う関連用語の追加」の場合は `type: "addition"` として必ず記録すること。
- 修正・追加・削除のいずれの場合も、可能であれば実際にブラウザで修正履歴タブを開き、意図した内容（日付・前後の文言・理由）が正しく表示されることを確認する（`python3 -m http.server` 等でローカル配信し、Playwright等で確認できる）。

## 用語を追加する際の既存用語の正当性チェックと関連語追加（重要）

`terms.json` に新しい用語を追加する指示を受けたときは、追加作業と**同時に**、以下を行うこと（カテゴリ見直し・穴埋めクイズ見直しと並ぶ、追加時の必須チェック項目の一つ）。

1. 追加する用語と関連する既存用語（同じ概念を扱う用語、定義文中で相互に参照している用語、同じ制度・指数・規制を扱う用語など）について、**カテゴリ分類だけでなく記述内容そのものの正しさ**を確認する。特に次の観点に注意する。
   - 現在の制度・市場慣行と整合しているか（規制の適用開始日、指数の算出方法変更など、時間とともに変わりうる事実関係）。
   - 他の用語の定義と論理的に矛盾していないか（例：ある用語の定義が、別の用語の定義で明示的に否定されている内容と矛盾していないか）。
   - 事実確認が必要な場合は `WebSearch` 等で裏取りし、可能であれば参考文献として提示できる情報源を確認する。
2. 確認の結果、既存用語の記述に誤り・古い情報が見つかった場合は `terms.json` の該当 `d` を修正し、前セクション「terms.jsonの修正・追加・削除を行った際の履歴記録」に従って `changelog.json` に `type: "correction"` のエントリを記録する。誤りの修正ではなく、他の用語と実質的に重複している・用語集の対象外と判断できるなど、用語そのものを残すべきでないと判断した場合は `terms.json` から削除し、`type: "deletion"` のエントリを記録する。
3. その修正によって、読者の理解に必要な前提知識・周辺概念（今回の修正で初めて登場した用語など）が新たに生じていないかを検討し、必要であれば関連用語として `terms.json` に追加する（カテゴリ見直し・参考文献更新・穴埋めクイズ見直しの各ルールに従う）。追加した用語は `changelog.json` に `type: "addition"` のエントリとして、どの修正がきっかけで追加したかが分かるよう `reason` に明記して記録する。
4. 上記の結果を `main` ブランチにpushすれば、用語集タブ・修正履歴タブの両方にGitHub Pages経由で自動的に反映される。
